
block=blocks.dragon_head
rot=0
active=1

fun onTick() -> Null {
    if (isBlock(blocks.air,0,2,0))
        active=0
    else if (isBlock(blocks.dragon_head,0,2,0))
        active=1

    if active {
        setBlock(block+rot,0,2,0)
        rot= (rot+1) % 16
    }
}
