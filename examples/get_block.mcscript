fun onTick() {
    run for @p at @s {
        block = getBlock(0, -0.05, 0)
        actionbar(block)
        # actionbar(block)
        if (not setBlock(block, 0, 10, 0))
            actionbar("Could not place block!")
    }
}